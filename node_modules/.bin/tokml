#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@maphubs/tokml/tokml" "$@"
  ret=$?
else 
  node  "$basedir/../@maphubs/tokml/tokml" "$@"
  ret=$?
fi
exit $ret
